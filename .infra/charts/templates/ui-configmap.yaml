{{- if and .Values.env .Values.ui.env.config }}
{{- if kindIs "map" .Values.ui.env.config }}
{{- if gt (len .Values.ui.env.config) 0 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-ui-config
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.ui.env.config }}
  {{ $key }}: {{ $value | toString | quote }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
